# Example only. Do not commit real secrets.
# Preferred command:
# kubectl -n secure-ticketing create secret generic ticketing-secrets --from-literal=POSTGRES_PASSWORD='<strong-password>'
apiVersion: v1
kind: Secret
metadata:
  name: ticketing-secrets
  namespace: secure-ticketing
  labels:
    app.kubernetes.io/name: secure-event-ticketing
type: Opaque
stringData:
  POSTGRES_PASSWORD: "<replace-with-strong-password>"
